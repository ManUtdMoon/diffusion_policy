enable: false

reward_scale: 0.05
reward_clip: 20.0

discriminator:
  hidden_dim: 256
  num_layers: 3

training:
  batch_size: 64
  learning_rate: 1e-4
  gradient_steps: 32
  update_every: 1000
  min_samples_per_class: 64
